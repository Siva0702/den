services:
  - type: web
    name: den-quant-scanner
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
